{
  "name": "agent-link",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bunx @tailwindcss/cli -i src/public/tailwind.input.css -o src/public/styles.css --watch & bun --watch src/server.ts",
    "build:css": "bunx @tailwindcss/cli -i src/public/tailwind.input.css -o src/public/styles.css --minify"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "latest",
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "@types/bun": "^1.3.11",
    "tailwindcss": "^4.2.2"
  }
}
